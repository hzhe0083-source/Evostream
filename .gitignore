README_compare.md
README_bk.md
README_bak.md
/model/
assets/benchmark_table.png

# Python cache
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Training outputs
/checkpoints/
/evaluation.json

# Conductor control room (session-local state; not shared via git)
/.conductor/

# Session / tooling state (local only, not part of the release)
# NOTE: root-anchored so sglang/.claude/ etc. stay tracked
/.claude/
/.grill/
.sii/
/.vscode/
/INVARIANTS.md

# Scratch / intermediate drafts (keep locally, don't ship)
/1.txt
/image.png
/streaming_benchmark_grid.png

# Local realtime inference drafts (not distributed)
/realtime_inference/API_SERVICE_zh.md

# Local realtime inference media (not distributed)
/realtime_inference/data/BV1qo4y1872f_aerial-xinjiang_1080P.mp4
